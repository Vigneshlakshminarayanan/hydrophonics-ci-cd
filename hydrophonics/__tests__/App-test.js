/**
 * @format
 */

 import 'react-native';
 import React from 'react';
 import App from '../App';
 
 // Note: test renderer must be required after react-native.
 import renderer from 'react-test-renderer';
 
 it('renders the app correctly', () => {
   renderer.create(<App />);
 });
 
 it('displays welcome screen', () => {
   renderer.create(<App />);
 });
 
 it('renders header title text correctly', () => {
   renderer.create(<App />);
 });
 
 it('renders header image correctly', () => {
   renderer.create(<App />);
 });
 
 it('renders description text correctly', () => {
   renderer.create(<App />);
 });
 
 it('optimum memory utilization', () => {
   renderer.create(<App />);
 });